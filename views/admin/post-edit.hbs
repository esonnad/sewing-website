<div class="page-title">
  <h1 class="page-title">Admin - Posts bearbeiten</h1>
</div>
<section class="card-container">
  <p>
    <div class="card" style="width: 45rem;">
      <div class="card-body">
        <form action="/admin/allPosts/editinfo/{{post._id}}" method="post">
          <label for="name">Titel</label><br>
          <input type="text" name="header" id="header" value="{{post.header}}">
          <br><br>
          <label for="name">Inhalt</label><br>
          <input type="text" name="content" id="content" value="{{post.content}}">
          <br><br>
          <button type="submit" class="btn2 btnAll">Aktualisieren</button>
        </form>
      </div>
    </div>
    <div class="card" style="width: 45rem;">
      {{#if post.imgPath}}
      <img src="{{ post.imgPath }}" alt="{{ post.imgName }}" class="card-img-top" />
      {{/if}}
      <div class="card-body">
        <form action="/admin/allPosts/editfile/{{post._id}}" method="post" enctype="multipart/form-data">
          <label for="photo">Bild ändern:</label>
          <input type="file" name="photo" id="photo">
          <br><br>
          <button type="submit" class="btn2 btnAll">Aktualisieren</button>
        </form>
      </div>
    </div>
  </p>
</section>