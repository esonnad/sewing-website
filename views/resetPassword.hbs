<div class="page-title">
  <h1 class="page-title">Passwort zurücksetzen!</h1>
</div>
<p>
  <div class="card" style="width: 30rem;">
    <div class="card-body">
      <h5 class="card-title">Passwort zurücksetzen</h5>
      {{#if message }}
      <div class="error-message">{{ message }}</div>
      {{/if}}
      <p>
        <form action="/reset/{{token}}" method="post">
          <label for="newPassw">Neues Passwort:</label>
          <input type="password" name="newPassw" id="newPassw" placeholder='New password' autofocus=true><br>
          <label for="confirmPassw">Passwort bestätigen:</label>
          <input type="password" name="confirmPassw" id="confirmPassw" placeholder='Confirm password'><br>
          <button type="submit">Passwort bestätigen</button>
        </form>
      </p>
    </div>
  </div>
</p>